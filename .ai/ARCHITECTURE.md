# Architecture

## Observed structure
The current repository is primarily a static/web portal structure with multiple HTML entry pages and an `assets/` directory.

## Boundary
This document records only repository-observed structure. Inspect source before making claims about backend integrations, authentication guarantees, deployment state, or data flows.
